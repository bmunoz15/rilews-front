// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#f6f6f6',
    },
    text:{
      primary: '#000',
      secondary: '#fff',
    },
  },
});

export default theme;
