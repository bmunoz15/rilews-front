// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#f6f6f6',
    },
    primary: {
      main: '#fff',
    },
    secondary: {
      main: '#f50057',
    },
  },
});

export default theme;
